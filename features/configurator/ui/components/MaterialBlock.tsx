import React from 'react';
import { MaterialComponent } from '../../model';

const MaterialBlock = ({data}: { data: MaterialComponent }) => {
    return (
        <div>
            MaterialComponent
        </div>
    );
};

export default MaterialBlock;